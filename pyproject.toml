[project]
name = "webscraping-goodreads"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { email = "bellaviana4@gmail.com" }
]
requires-python = ">=3.10"
dependencies = [
    "numpy>=2.2.6",
    "pandas>=2.3.3",
    "plotly>=7.0.0",
]

[project.scripts]
webscraping-goodreads = "webscraping_goodreads:main"

[build-system]
requires = ["uv_build>=0.12.12,<0.13.0"]
build-backend = "uv_build"
